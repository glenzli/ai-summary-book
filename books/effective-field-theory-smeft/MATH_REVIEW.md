# 数学与物理审查

审查日期：2026-07-08

## 当前结论

第一版草稿已经扩展并收口为内部定稿：EFT/SMEFT 的核心定义链、典型 EFT 例子、树级匹配、Warsaw basis 目录、flavor/CP 纪律、RGE 最小例子、一圈匹配显式环积分、Higgs 观测量推导、维数八有效性边界、现代 SMEFT 方向地图、flavor 参数计数、输入方案 worked example、observables-to-operators 图谱、响应矩阵和基准数值矩阵均已纳入。严格性达到“第一版内部收口稿”状态。

## 已通过项目

1.  每章包含目标、前置知识、小结和练习。
2.  EFT 的自由度、对称性、尺度、截断和误差纪律已明确。
3.  SMEFT 默认口径为线性电弱实现，未与 HEFT 混用。
4.  Warsaw basis 的 59 个结构已列出，并保留原始文献为外部输入。
5.  RGE 给出最小书内例子，完整矩阵保留为外部输入。
6.  一圈匹配已有显式环积分例子。
7.  维数八、unitarity 和 positivity 已作为截断边界纳入。
8.  现代 SMEFT 方向已有算符地图和元数据标准。
9.  已建立算符与命题台账以及出版收口审计。
10. Flavor 参数计数已有通用线性代数推导。
11. 电弱输入方案已有完整线性 worked example。
12. 基变换已有接口台账和三个破缺相例子。
13. 章节收口审计已标明每章是否仍需出版级增强。
14. 响应矩阵已有 EWPO、Higgs、高能 dilepton 和 flavor 的 worked examples。
15. 基变换已有附录 F 细表。
16. Warsaw flavor/Fierz 计数已有可审计算法。
17. 基准数值响应矩阵已给出，不依赖外部实验数据。

## 外部边界

1.  标准模型规范固定和破缺相 Feynman 规则只给出接口。
2.  flavor 参数计数已有通用表和算法，但 Warsaw exact 2499 的逐项矩阵秩表未内化。
3.  实验拟合已有符号和基准数值响应矩阵，真实数据表、协方差或数值复现属于外部接口。
4.  Warsaw basis 与其他基之间已有接口台账和细表，但工具级全量转换表属于外部接口。
5.  现代 SMEFT 方向已有地图和图谱，逐方向数值章节属于第二版扩展。

## 收口结论

按教材自身结构，本书达到第一版内部收口。若转向研究参考手册，还可继续加入真实数据协方差、全量转换表、完整维数八基和全局拟合复现脚本。
